# project-service-prod.yml - env-var references only (Chapter 3 rules)

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/project_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    org.hibernate.SQL: WARN
