# project-service-dev.yml

spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/project_db
    username: taskflow
    password: taskflow_local_dev

logging:
  level:
    org.hibernate.SQL: DEBUG
