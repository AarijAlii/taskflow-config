# ai-service-dev.yml

taskflow:
  groq:
    api-key: ${GROQ_API_KEY:}
    # Empty default = degraded mode (gRPC FAILED_PRECONDITION, translated
    # to HTTP 503 by task-service). Set GROQ_API_KEY in your shell or a
    # .env file next to docker-compose.yml to enable for real.
